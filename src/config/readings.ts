// config/readings.ts
import type { ReadingRegistry } from '../types/reading'
import { log_笔记本 } from './items/reading'

export const readingRegistry: ReadingRegistry = {
  readings: {
    log_笔记本,
  },
}